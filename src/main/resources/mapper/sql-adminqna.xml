<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sidea.version.nudge.mapper.AdminQnaMapper">

    <!-- 미작업 -->
    <!-- Qna 목록 조회 -->
    <select id="getQnaList" parameterType="long">
        select user_email, user_pw, user_nickname, user_image, user_point_balance, user_donation_balance, user_leave
        from ask
        where user_idx = #{userIdx} and user_pw = #{userPw}
    </select>

    <!--  QNA 상세 조회  -->
    <select id="getQnaDetail" parameterType="sidea.version.nudge.dto.AskDto" resultType="sidea.version.nudge.dto.AskDto">
        select user_email, user_pw, user_nickname, user_image, user_point_balance, user_donation_balance, user_leave
        from ask
        where ask_idx = #{askIdx}
    </select>

    <!--  QNA 등록  -->
    <insert id="insertQna" parameterType="sidea.version.nudge.dto.AskDto">
        insert into ask (user_email, user_pw, user_nickname, user_image, user_point_balance, user_donation_balance, user_leave)
        values (#{userEmail}, #{userPw}, #{userNickname}, #{userImage}, #{userPointBalance}, #{userDonationBalance}, #{userLeave})
    </insert>

    <!--  QNA 수정  -->
    <update id="updateQna" parameterType="sidea.version.nudge.dto.AskDto">
        update ask set user_leave = "Y"
        where ask_idx = #{askIdx}
    </update>

    <!--  QNA 삭제  -->
    <update id="deleteQna" parameterType="sidea.version.nudge.dto.AskDto">
        update ask set user_leave = "Y"
        where ask_idx = #{askIdx}
    </update>
</mapper>