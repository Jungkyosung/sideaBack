<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sidea.version.nudge.mapper.DonationMapper">

<!--  기부리스트 조회  -->
    <select id="getDonationList" resultType="sidea.version.nudge.dto.DonationInfoDto">
        select *
        from donation dn
        join donor dr
        on dn.donor_idx = dr.donor_idx
        where donation_delete = "N" and donation_duration &gt; now()
    </select>

<!--  기부세부내역 조회  -->
    <select id="getDonationDetail" parameterType="Long" resultType="sidea.version.nudge.dto.DonationInfoDto">
        select *
        from donation dn
        join donor dr
        on dn.donor_idx = dr.donor_idx
        where donation_idx = #{donationIdx}
    </select>

    <!--  참여 리스트 조회(작업 중)  -->
    <select id="getDonationListByUser" parameterType="Integer" resultType="sidea.version.nudge.dto.DonationInfoDto">
        select * from user_donation ud
        join donation d
        on ud.donation_idx = d.donation_idx
        join user u
        on u.user_idx = ud.user_idx
        where u.user_idx = #{userIdx}
    </select>




    <insert id="registUser" parameterType="sidea.version.nudge.dto.UserDto">
        insert into user (user_email, user_pw, user_nickname, user_image, user_point_balance, user_donation_balance, user_leave)
        values (#{userEmail}, #{userPw}, #{userNickname}, #{userImage}, #{userPointBalance}, #{userDonationBalance}, #{userLeave})
    </insert>

    <update id="userLeave" parameterType="sidea.version.nudge.dto.UserDto">
        update user set user_leave = "Y"
        where user_email = #{userEmail}
    </update>
</mapper>